import test from 'node:test'

test('store contracts are type-only modules', () => {
    // Contract types are validated at compile time via implementations.
    // Provider-specific parity tests live in their respective packages.
})
