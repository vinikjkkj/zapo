/**
 * Layer 1 — generated protobuf wrapper.
 *
 * Re-exports the proto namespace from zapo-js. The protos are generated from
 * `.proto` definitions, so they are not "lib behavior" and are safe to reuse
 * directly. See AGENTS.md §3.
 */

export { proto } from 'zapo-js/proto'
export type { Proto } from 'zapo-js/proto'
